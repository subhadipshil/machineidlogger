<Page
    x:Class="MachineIDLogger.Views.DisplaysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MachineIDLogger.ViewModels"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" Padding="20,16,24,24">
            <StackPanel Spacing="2">
                <TextBlock Text="Displays &amp; Monitors" FontSize="20" FontWeight="SemiBold" />
                <TextBlock Text="Connected display monitors, native resolutions, refresh frequencies, and video output ports" 
                           FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}" />
            </StackPanel>

            <ListView ItemsSource="{x:Bind ViewModel.Displays, Mode=OneWay}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource TechnicalCardStyle}" Margin="0,0,0,12">
                            <StackPanel Spacing="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE7F4;" FontSize="15" Foreground="{ThemeResource AccentFillColorDefaultBrush}" />
                                        <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" />
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding ConnectionType}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondary}" />
                                </Grid>

                                <Grid ColumnSpacing="16" RowSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="140" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="140" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Resolution" Style="{StaticResource ValueTextStyle}" Foreground="{ThemeResource TextFillColorSecondary}" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Resolution}" Style="{StaticResource ValueTextStyle}" FontWeight="SemiBold" />

                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Refresh Rate" Style="{StaticResource ValueTextStyle}" Foreground="{ThemeResource TextFillColorSecondary}" />
                                    <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding RefreshRateHz}" Style="{StaticResource ValueTextStyle}" />

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Manufacturer" Style="{StaticResource ValueTextStyle}" Foreground="{ThemeResource TextFillColorSecondary}" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Manufacturer}" Style="{StaticResource ValueTextStyle}" />

                                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Device Name" Style="{StaticResource ValueTextStyle}" Foreground="{ThemeResource TextFillColorSecondary}" />
                                    <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding DeviceName}" Style="{StaticResource MonospaceValueStyle}" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
