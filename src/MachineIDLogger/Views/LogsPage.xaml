<Page
    x:Class="MachineIDLogger.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MachineIDLogger.Core.Models"
    xmlns:vm="using:MachineIDLogger.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="20,16,24,24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="2">
                <TextBlock Text="Application &amp; Audit Logs" FontSize="20" FontWeight="SemiBold" />
                <TextBlock Text="Structured system inspection and machine identity modification event logs" 
                           FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Command="{x:Bind ViewModel.ClearLogsCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE74D;" FontSize="12" />
                        <TextBlock Text="Clear" FontSize="12" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Search & Filter Toolbar -->
        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="160" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" PlaceholderText="Search log messages or exceptions..." 
                     Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <ComboBox Grid.Column="1" HorizontalAlignment="Stretch"
                      ItemsSource="{x:Bind ViewModel.SeverityOptions}"
                      SelectedItem="{x:Bind ViewModel.SelectedSeverity, Mode=TwoWay}" />
        </Grid>

        <!-- Log List -->
        <Border Grid.Row="2" Style="{StaticResource TechnicalCardStyle}" Padding="0">
            <ListView ItemsSource="{x:Bind ViewModel.LogEntries, Mode=OneWay}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:LogEntry">
                        <Grid Padding="12,6" BorderBrush="{ThemeResource CardStrokeColorDefault}" BorderThickness="0,0,0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="170" />
                                <ColumnDefinition Width="90" />
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="40" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{x:Bind TimestampDisplay}" FontFamily="{StaticResource MonospaceFont}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondary}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{x:Bind Severity}" FontSize="11" FontWeight="SemiBold" 
                                       Foreground="{x:Bind Severity, Converter={StaticResource DiagnosticStatusToBrushConverter}}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{x:Bind Category}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiary}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="3" Text="{x:Bind Message}" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center" IsTextSelectionEnabled="True" />
                            <Button Grid.Column="4" Tag="{x:Bind}" Click="CopyLog_Click" Padding="4" HorizontalAlignment="Right" ToolTipService.ToolTip="Copy log entry">
                                <FontIcon Glyph="&#xE8C8;" FontSize="10" />
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
