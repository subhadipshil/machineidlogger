<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="MachineIDLogger.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MachineIDLogger.Converters"
    xmlns:local="using:MachineIDLogger">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <converters:ByteFormatConverter x:Key="ByteFormatConverter" />
            <converters:PercentageFormatConverter x:Key="PercentageFormatConverter" />
            <converters:DeviceCountConverter x:Key="DeviceCountConverter" />
            <converters:DiagnosticStatusToBrushConverter x:Key="DiagnosticStatusToBrushConverter" />
            <converters:BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
            <converters:BooleanToVisibilityConverter x:Key="InvertBoolToVisConverter" Invert="True" />
            <converters:StringToBoolConverter x:Key="StringToBoolConverter" />
            <converters:BooleanNegationConverter x:Key="BoolNegationConverter" />
            <converters:DiagnosticButtonTextConverter x:Key="DiagnosticButtonTextConverter" />

            <!-- Typography & Technical styling -->
            <FontFamily x:Key="MonospaceFont">Cascadia Mono, Consolas, Courier New</FontFamily>
            
            <Style x:Key="TechnicalCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefault}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefault}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="Padding" Value="14" />
            </Style>

            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondary}" />
                <Setter Property="CharacterSpacing" Value="40" />
            </Style>

            <Style x:Key="ValueTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="FontWeight" Value="Normal" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimary}" />
                <Setter Property="IsTextSelectionEnabled" Value="True" />
            </Style>

            <Style x:Key="MonospaceValueStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource MonospaceFont}" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimary}" />
                <Setter Property="IsTextSelectionEnabled" Value="True" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
